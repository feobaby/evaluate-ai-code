{
  "name": "evaluate-ai-code",
  "version": "1.0.0",
  "description": "Authentication API with Sign Up and Sign In (JWT, bcrypt, Sequelize, PostgreSQL)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/"
  },
  "keywords": ["auth", "jwt", "express", "sequelize", "postgresql"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.35.2"
  },
  "devDependencies": {
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=18"
  }
}
